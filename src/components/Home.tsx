import React from 'react';
import Feed from './feed/Feed';

const Home = () => {
  return (
    <section className="container mainContainer">
      <Feed user={null} />
    </section>
  );
};

export default Home;
